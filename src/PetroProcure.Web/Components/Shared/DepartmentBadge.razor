<MudChip T="string" Class="department-badge" Color="Color.Primary" Variant="Variant.Outlined" Icon="@Icon">@Title</MudChip>
@code {
    [Parameter] public string Title { get; set; } = "";
    [Parameter] public string Icon { get; set; } = Icons.Material.Filled.Apartment;
}
